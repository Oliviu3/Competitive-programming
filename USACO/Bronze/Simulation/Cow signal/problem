

#include <vector>
#include <algorithm>
#include <cstdio>
#include <iostream>
#include <set>

using namespace std;

int main()
{
  freopen("cowsignal.in", "r", stdin);
  freopen( "cowsignal.out", "w", stdout);
 int m,n,k;
    string s;
    cin>>m>>n>>k;
    for(int i=0;i<m;i++)
    {
        cin>>s;
        for(int j=0;j<k;j++)
        {
            for (int g=0;g<n;g++)
            {
               for (int h=0;h<k;h++)
               {
                   cout<<s[g];
               }
            }
            cout<<endl;
        }
    }
  }
